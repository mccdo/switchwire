#
# This module is provided as switchwire_USE_FILE by switchwireConfig.cmake.  It can
# be INCLUDEd in a project to load the needed compiler and linker
# settings to use switchwire.
#

IF(NOT switchwire_USE_FILE_INCLUDED)
  SET(switchwire_USE_FILE_INCLUDED 1)

  # Update CMAKE_MODULE_PATH so includes work.
  #SET (CMAKE_MODULE_PATH
  #  ${CMAKE_MODULE_PATH}
  #  ${switchwire_CMAKE_DIR})

  # Add compiler flags needed to use switchwire.
  #SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${switchwire_REQUIRED_C_FLAGS}")
  #SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${switchwire_REQUIRED_CXX_FLAGS}")
  #SET(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${switchwire_REQUIRED_EXE_LINKER_FLAGS}")
  #SET(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} ${switchwire_REQUIRED_SHARED_LINKER_FLAGS}")
  #SET(CMAKE_MODULE_LINKER_FLAGS "${CMAKE_MODULE_LINKER_FLAGS} ${switchwire_REQUIRED_MODULE_LINKER_FLAGS}")

  # Add include directories needed to use switchwire.
  INCLUDE_DIRECTORIES(${switchwire_INCLUDE_DIRS})

ENDIF(NOT switchwire_USE_FILE_INCLUDED)
